# T1.6 Scale Factor Reduction Experiment Report

**Date:** 2026-03-29
**Status:** Harness implemented, empirical run blocked by local environment

## Goal

Evaluate the tradeoff of lowering CKKS `global_scale` from `2^40` to `2^35` and `2^30`:

1. HE matmul speed
2. End-to-end accuracy tests
3. 2048x2048 matmul error growth

## What Was Added

To make this experiment executable in-repo, the following support was added:

- `client/encryption/ckks_context.py`
  Adds `load_ckks_config()` and a `global_scale_override` path for context creation.

- `benchmarks/bench_he_matmul.py`
  Adds `--config-path` so the benchmark can run against temporary CKKS configs with alternate scales.

- `client/tests/test_e2e_accuracy.py`
  Reads `ZKLLM_CKKS_SCALE` so the same test file can be exercised at different scales without copying it.

- `benchmarks/scale_experiment.py`
  New experiment runner that:
  - sweeps `2^40`, `2^35`, `2^30`
  - runs `bench_he_matmul.py` for each scale
  - runs `client/tests/test_e2e_accuracy.py` for each scale
  - measures 2048x2048 synthetic matmul error
  - writes a structured JSON report

## Local Blocker

I was not able to execute the experiment on this machine because the available `python3` environment is missing the tooling required to install the project dependencies:

- `python3 -m pip` is unavailable
- `python3 -m venv` fails because `ensurepip` / `python3-venv` is not installed

That means I could not install `tenseal`, `numpy`, `pytest`, and the rest of the runtime stack needed to produce real empirical results in this session.

## How To Run Once Environment Exists

```bash
python benchmarks/scale_experiment.py
```

This writes:

- `benchmarks/results/scale_experiment.json`
- per-scale matmul benchmark JSON files under `benchmarks/results/`

## Provisional Recommendation

Do **not** change the default production scale from `2^40` based only on theory.

Given the already-corrected estimate in the action plan, the expected upside is modest:

- `2^35`: plausible low-risk experiment target
- `2^30`: likely no-go unless empirical accuracy remains comfortably inside tolerance and the measured speedup is materially better than noise

Until the harness is run in a proper environment, the correct Phase 1 decision is:

- keep `2^40` as the baseline,
- treat `2^35` and `2^30` as experimental configurations only,
- require measured benchmark and accuracy evidence before changing defaults.
