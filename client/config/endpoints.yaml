# Server endpoint configuration
# AUTH_TOKEN is loaded from environment: ZKLLM_SERVER_AUTH_TOKEN or AUTH_TOKEN
server:
  base_url: "http://localhost:8000"
  infer_endpoint: "/api/infer"
  session_endpoint: "/api/session"
  layer_endpoint: "/api/layer"
  layer_ws_endpoint: "/api/layer/ws"
  # auth_token loaded from env var - do not hardcode
